# dependencies
node_modules/

# build output (tsc dist, esbuild bundles, skill artifacts)
dist/
dist-bundle/
*.tsbuildinfo

# secrets & env
.env
.env.*
!.env.example
.dev.vars

# runtime data — never commit vaults, tokens, or local governance DBs
.socials-data/
.socials-test/
digests/
*.db
*.db-journal
*.db-shm
*.db-wal
controlkeel/

# packaging artifacts
*.tgz
*.skill

# tooling caches
.wrangler/
coverage/
.DS_Store
